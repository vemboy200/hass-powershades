pytest-homeassistant-custom-component==0.13.365
# TODO: switch back to a PyPI version pin (pyowershades==0.4.0) once that
# release is actually published - pinned to a commit in the meantime.
pyowershades @ git+https://github.com/vemboy200/Pyowershades.git@551ece7
